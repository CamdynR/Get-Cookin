{
  "scripts": {
    "build": "node build/create-dist.js && node build/minify.js",
    "lint": "",
    "pre-commit-msg": "echo \"Pre-commit checks...\" && exit 0"
  },
  "pre-commit": [
    "pre-commit-msg",
    "build"
  ],
  "devDependencies": {
    "clean-css": "^4.2.4",
    "fs-extra": "^10.0.0",
    "html-minifier": "^4.0.0",
    "pre-commit": "^1.2.2",
    "uglify-js": "^3.14.3"
  }
}
